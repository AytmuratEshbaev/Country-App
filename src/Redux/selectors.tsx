export const selectTheme = (state: { theme: string }) => state.theme;
export const searchValue = (state: { search: string }) => state.search;
export const selectRegion = (state: { region: string }) => state.region;
export const selectCountriesData = (state: { data: any[] }) => state.data;